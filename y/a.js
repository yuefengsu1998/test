
let b ='sdsd'

let a = 'yu'

git clone '地址'

git pull origin '分支名'

git add .

git commit -m 'xxxx'

git push origin '分支名'

git branch '分支名'

git branch -a

git branch -f

git checkout '分支名'

git merge '分支名'

git branck -d '分支名'

git push origin delete '分支名'

git status
